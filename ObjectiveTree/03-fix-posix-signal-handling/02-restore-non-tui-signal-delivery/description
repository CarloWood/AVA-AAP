Define and implement an explicit non-TUI signal policy before mode-specific work begins, so print, RPC, ACP, line-shell, and other non-TUI commands respond to SIGINT and SIGTERM without requiring TUI signal-bit consumers. Preserve the early blocked-callback policy for the TUI, maintain utility registration bookkeeping, and verify frontend selection, terminal-owning prompts, and pending startup signals.
