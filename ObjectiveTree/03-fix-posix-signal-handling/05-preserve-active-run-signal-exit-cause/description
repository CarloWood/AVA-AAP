Ensure a POSIX signal observed during active-run input polling is atomically claimed and handled as a signal rather than an indistinguishable Ctrl-C key event. Preserve terminal_signal_received through active-run, submit, and composer outcomes whenever the signal requests exit, including polling and completion boundaries, while keeping genuine keyboard input and locally handled interrupts distinct.
