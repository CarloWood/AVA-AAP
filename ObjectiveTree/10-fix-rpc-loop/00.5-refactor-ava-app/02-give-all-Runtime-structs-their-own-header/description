Move ContextSourceMetadata, RuntimeFreshnessSourceMetadata (plus add RuntimeFreshnessSourceKind), RuntimePromptOverrides, RuntimeReasoningSelection and RuntimeBasePromptMetadata each in their own header file in src/ava/app with that same name plus .h. Make sure each includes the headers they need (but no more). Remove the file that currently defines them (src/ava/app/runtime_types.h). Fix all includes so the project compiles again.
