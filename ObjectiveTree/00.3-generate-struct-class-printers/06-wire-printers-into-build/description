Add the generated printer source(s) from goal 05 to the ava_debug library via target_sources so the project compiles and links, and arrange that a normal build auto-generates them only when they are absent (fresh checkout). Ensure a no-op build incurs no generation cost, with regeneration happening solely through the manual ctags-json / generation chain exposed by goals 03 and 05.
