Reconcile the codebase after reordering members of `struct OpenOptions` (src/ava/app/runtime/OpenOptions.h): `mode`, `tool_visibility`, `paths`, `sessionless`, and `offline` now sit immediately after `current_dir` (see `git diff`). Regenerate the print_members codegen with `aap-build --target generate-print-members` (OpenOptions uses `AVA_DEBUG_PRINT_MEMBERS_ON`), and at every `OpenOptions` construction site, reorder the member initializers to match the new declaration order — both positional aggregate initializers (although I don't think those exist) bit ALSO designated initializers. Verify by scanning through the diff afterwards.
