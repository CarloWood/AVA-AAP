1. Decide how `Window` should coordinate displaying an independent `Pad` in its writable area - needs discussion
2. Generalize `ParagraphPad` into a `Pad` containing `HorizontalLayout` objects while retaining convenient paragraph appends - needs discussion
3. Design a geometric vector allocator using `MemoryPagePool` size classes with heap fallback - needs discussion
